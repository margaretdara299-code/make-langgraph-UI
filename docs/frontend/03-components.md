# 🧱 3. Component Architecture
- Use functional components solely.
- Logic belongs to `/hooks` and `/services`, keeping components largely declarative.
- Reusable primitives live in `/components` (e.g. ActionCards, PropertyDrawers).